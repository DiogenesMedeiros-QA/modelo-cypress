# Responsibility: Example env variables for local runs / CI mapping
# Copy to .env or set CI variables accordingly

# Cypress base URL for API under test
CYPRESS_baseUrl=http://localhost:3000

# Database connection URL (Postgres example)
# Format: postgres://user:password@host:port/dbname
DB_URL=postgres://test:test@localhost:5432/testdb
DB_CLIENT=pg

# Optional token for API auth
CYPRESS_TOKEN=
